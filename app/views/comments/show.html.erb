<h1>Comment Details</h1>

<h2>Article: <%= @article.title %></h2>
<p><%= @article.content %></p>

<div class="comment">
  <p><strong><%= @comment.user.username %>:</strong> <%= @comment.content %></p>
  <p><em>Posted on: <%= @comment.created_at.strftime("%B %d, %Y at %I:%M %p") %></em></p>
  
  <% if user_signed_in? && @comment.user == current_user %>
    <%= link_to 'Edit', edit_article_comment_path(@article, @comment), class: 'btn btn-secondary' %>
    <%= link_to 'Delete', article_comment_path(@article, @comment), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
  <% end %>
</div>

<%= link_to 'Back to Article', article_path(@article), class: 'btn btn-secondary' %>
