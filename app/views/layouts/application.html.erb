<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags%>

  </head>

  <body>
    <nav class='mx-4 my-6 px-2 flex justify-between items-center'>
        <div class="flex items-center"> 
          <%if user_signed_in?%>

        <% if current_user.avatar.attached? %>
          <%= image_tag current_user.avatar.variant(resize_to_fill: [100, 100]) , class: "rounded-lg"%>

        <% else %>
          <p>Nothing</p>
      <% end %>


        <div class=''><span class=' p-2  rounded-md font-bold'><%= link_to current_user.username.upcase_first,edit_user_registration_path%></span></div>

        </div> 
        
             
        <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete,
    turbo_confirm: "Are you sure?"}, class: 'px-4 py-2 h-10 bg-red-500 text-white rounded hover:bg-red-600' %>
    <%else%>
      <div class='flex justify-end gap-x-2'>
        
      <div>
        <%= link_to 'Sign In',new_user_session_path ,class: 'text-black px-4 py-2 border border-neutral-400 rounded-md' %>
        <%= link_to 'Sign Up',new_user_registration_path ,class: 'px-4 py-2 text-white bg-black rounded-md border border-neutral-600' %>
      </div>
      </div>
    <%end%>
     
    </nav>
    <main class="container mx-auto  ">
    <div id="flash-messages"  class="transition-opacity duration-700 ease-in-out opacity-0">
      <p class="notice"><%= notice %></p>

    <p class="alert"><%= alert %></p>
  </div>
      <%= yield %>
    </main>
  </body>
</html>
